export enum Severity {
  Critical = 'Critical',
  Major = 'Major',
  Minor = 'Minor',
}

export enum Status {
  Todo = 'To do',
  InProgress = 'In Progress',
  InReview = 'In Review',
  Done = 'Done',
}

export enum TaskType {
  Bug = 'Bug',
  Feature = 'Feature',
  Task = 'Task',
}
